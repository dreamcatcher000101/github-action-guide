import React from "react";
import { withLayout } from "../../../hoc";

export const SignInViewOrigin: React.FC = () => {
  return <div>Sign In View</div>;
};

export const SignInView = withLayout(SignInViewOrigin);
